{
  "version": 2,
  "buildCommand": "npm install && chmod +x ./node_modules/.bin/tsc && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
} 